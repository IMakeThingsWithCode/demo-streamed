import { config } from "./config.mjs";

export class Extra {
    // everything not in the other ones
}

export class keyframePath {
    // tweenAllSingle
}

export class multiKeyframePath {
    // tweenAllMulti
}

export class campath {
    // add tweening, creation functions, etc.
    // move tweenallmulti for campaths here
}